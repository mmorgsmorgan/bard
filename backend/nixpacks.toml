# Railway build configuration for BARD backend
# Updated: 2026-05-30 - simplified for direct npm install

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"
